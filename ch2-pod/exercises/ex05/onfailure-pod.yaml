apiVersion: v1
kind: Pod
metadata:
  name: onfailure-pod
spec:
  containers:
    - name: failing-container
      image: busybox:latest
      command: ["/bin/sh", "-c", "sleep 10; exit 1"]
  restartPolicy: OnFailure
